import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marcopeluso.portfolio',
  appName: 'Marco Peluso',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
